import React from 'react';

const Header: React.FC = () => {
  return (
    <header>
      <h1>Jobs App</h1>
      {/* Add additional header content here */}
    </header>
  );
};

export default Header;